{% extends admin.getTemplate('base_list_field') %}

{% block field%}
    {% if value is empty %}
        &nbsp;
    {% else %}
        {{ value|date('d/m/Y H:i') }}
    {% endif %}
{% endblock %}